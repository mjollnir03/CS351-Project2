const user = require("./loginController");

const addToCart = async(product) => {
    // Connected to user
    // Add the product to user
}

const removeFromCart = async(product) => {
    // Connect to user
    // Remove the product from the user
}

const updateCart = async(product, quantity) => {

}

module.exports = {
    addToCart,
    removeFromCart,
    updateCart
};